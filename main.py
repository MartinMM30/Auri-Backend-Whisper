import os
import uvicorn
from server import app   # importa tu FastAPI principal

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 8000))
    uvicorn.run(app, host="0.0.0.0", port=port)
